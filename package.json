{
  "name": "nostr-binding",
  "version": "0.1.0-rc3",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "fmt": "lerna run fmt",
    "chore": "pnpm i && pnpm -r run fmt && pnpm -r run lint && pnpm -r run test && pnpm -r run build"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
