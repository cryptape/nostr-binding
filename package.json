{
  "name": "nostr-binding",
  "version": "0.1.0-rc3",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "fmt": "pnpm -r run fmt",
    "lint": "pnpm -r run lint",
    "chore": "pnpm i && pnpm -r run fmt && pnpm -r run lint && pnpm -r run test && pnpm -r run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
